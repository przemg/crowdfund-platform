import * as React from 'react';
import GenericLoadingIndicator from '.';

export default {
  title: 'Organisms/GenericLoadingIndicator',
  component: GenericLoadingIndicator,
};

const Template = () => <GenericLoadingIndicator />;

export const Playground = Template.bind({});

export const Default = () => <GenericLoadingIndicator />;
